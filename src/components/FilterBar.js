// @flow

import React, { Component } from 'react';

class FilterBar extends Component {
    render() {

        return (
            <nav>
                I'm the filter bar
            </nav>
        );
    }
}

export default FilterBar;
